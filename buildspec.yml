version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo "Using Node.js 14"
      - echo "No dependencies to install"
  build:
    commands:
      - echo "Running placeholder for Conformity scan"
      - cat template.yaml  # Replace or expand this when integrating the real scanner
artifacts:
  files:
    - template.yaml

