version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo "No npm install needed"
  build:
    commands:
      - echo "Running Conformity scan..."
      - cat template.yaml  # Replace this with actual scan command if integrated
artifacts:
  files:
    - template.yaml

